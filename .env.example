# Environment Variables (IMPORTANT: Never commit real keys!)
# Copy this file to .env.local and fill in your values
# .env.local is ignored by git and won't be uploaded

# MongoDB Database Connection
# Get from: mongodb.com/atlas
MONGODB_URI=your_mongodb_connection_string_here

# JWT Secret (generate a random string, min 32 characters)
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=generate_a_random_secret_key_here

# Groq API Key (optional, for AI features)
# Get from: console.groq.com
GROQ_API_KEY=your_groq_api_key_here

# Node Environment
NODE_ENV=production
